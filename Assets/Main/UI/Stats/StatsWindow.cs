using UnityEngine;

namespace Main.UI.Stats
{
    public class StatsWindow : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
